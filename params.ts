export interface ParsedItem {
  server: string;
  name: string;
  itemName: string;
  gold: number;
  price: number;
  rec: number;
}

export const chats: number[] = [];
export const last: ParsedItem[]  = [];
